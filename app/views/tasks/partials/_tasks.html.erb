<div class="tasks">
	<% tasks.each do |task| %>
		<%= render :partial => 'tasks/partials/view', :locals => {:task => task} %>
	<% end %>
</div>
<br>
<%= link_to('New Task', new_story_task_path(story), :class => 'btn btn-primary btn-xs',
	:remote => true, :'data-disable-with' => "Loading...") unless story.sealed_for_tasks? %>