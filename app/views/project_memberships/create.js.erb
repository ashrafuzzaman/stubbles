<% if @project_membership.persisted? %>
	$('#project_membership tbody').append('<%= escape_javascript(render :partial => 'row', :locals => {:membership => @project_membership, :project => @project}) %>');
	$('form').get(0).reset();
	$('tr:last').hide().slideDown();
<% end %>

<%= render 'shared/notification' %>