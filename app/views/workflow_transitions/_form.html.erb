<%= simple_form_for(@workflow_transition) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :event %>
    <%= f.association :from_status %>
    <%= f.association :to_status %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
