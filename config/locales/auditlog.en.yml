en:
  auditlog:
    models:
      nothing_in_the_past: Nothing in the past
      workflow_status_id:
        set: Set status to %{now}
        unset: Reset status from %{was}
        changed: Status changed from %{was} to %{now}
      assigned_to_id:
        set: Assigned to %{now}
        unset: Un assigned from %{was}
        changed: Assigned from %{was} to %{now}
      story:
        title:
          set: Set title to %{now}
          changed: Changed title from %{was} to %{now}