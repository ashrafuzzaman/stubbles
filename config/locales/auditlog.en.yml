en:
  auditlog:
    models:
      nothing_in_the_past: Nothing in the past
      task:
        assigned_to_id:
          set: Assigned to %{now}
          unset: Un assigned from %{was}
          changed: Assigned from %{was} to %{now}
        workflow_status_id:
          set: Set status to %{now}
          changed: Status changed from %{was} to %{now}